// import { BrowserRouter as Router, Routes, Route } from "react-router-dom";
import Route from "../routing/Route"; // Ensure the file exists at 'src/routing/route.tsx' or adjust the path accordingly
export default function App() {
  return (
    <div className="App">
      {/* Main content area */}
      <Route />
    </div>
  );
}
